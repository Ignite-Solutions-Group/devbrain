name: devbrain
hooks:
  postprovision:
    shell: pwsh
    run: |
      Write-Host "Waiting 30 seconds for Function App to warm up..."
      Start-Sleep -Seconds 30
      Write-Host "Done. Starting deployment."
services:
  api:
    project: src/DevBrain.Functions
    language: dotnet
    host: function